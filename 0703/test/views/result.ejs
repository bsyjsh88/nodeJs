<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h4>데이터 전송 결과</h4>
    <h4><%= title  %></h4>
    <div>이름 : <span><%= userInfo.name %></span>님 환영합니다</div>
    <div>성별 : <span><%= userInfo.gender %> </span></div>
    <div>생년월일 : <span> <%= userInfo.year %>년</span> 
        <span> <%= userInfo.mon %>월</span>
        <span> <%= userInfo.day %>일</span></div>
    <div>취미 : <span><%= userInfo.interest %></span></div>
    <div><a href="./form.ejs">홈</a></div>
</body>
</html>